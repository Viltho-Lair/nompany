{
  "//": "TypeScript covers everything server-side — platform, modules, shared, lib, and all 153 API routes. What is left in JavaScript is the 272 browser files under src/components and src/app, which convert with the UI work in Wave 4: allowJs lets a .tsx import one of them, and checkJs keeps the compiler from grading files nobody wrote types for. This line said the reverse ('TypeScript covers the document application only; the rest stays plain JavaScript') long after Wave 3 made it false. See tsconfig.strict.json, which grades every .ts/.tsx with noImplicitAny on.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "checkJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noImplicitAny": false,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "//exclude": "services/ is EXCLUDED because it is not this application. Each folder under it is a separately deployed program with its own package.json, its own dependencies and its own tsconfig — services/pg-gateway needs @google-cloud/cloud-sql-connector, which the app neither has nor should have. `include` here is `**/*.ts`, so without this line the app's typecheck grades a service against dependencies that are not in the app's node_modules and fails on imports that are perfectly correct where they live. `next build` was never affected (Next compiles only what src/app imports); `tsc` was.",
  "exclude": [
    "node_modules",
    "tests",
    "services"
  ]
}
