{
  "name": "@nompany/pg-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Cloud Run service that reaches Cloud SQL for Vercel. One POST /tx call is one transaction. Its dependencies are ITS OWN — @google-cloud/cloud-sql-connector must never appear in the app's package.json, which is why services/ is excluded from the root tsconfig.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "build": "esbuild src/container-entry.ts --bundle --platform=node --format=esm --target=node22 --packages=external --outfile=dist/main.mjs",
    "start": "npm run build && node dist/main.mjs",
    "test": "node test/gateway.test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@google-cloud/cloud-sql-connector": "^1.9.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/pg": "^8.23.1",
    "esbuild": "0.25.10",
    "typescript": "^5.9.3"
  }
}
