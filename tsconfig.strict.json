{
  "//": [
    "THE RATCHET. The repo-wide tsconfig runs with checkJs false and noImplicitAny",
    "false, because grading a hundred files nobody wrote types for produces a",
    "number so large it gets ignored. This config grades only the folders that",
    "have been converted, with noImplicitAny ON, and CI runs both.",
    "",
    "A folder is added to `include` when its wave lands and never removed. That is",
    "the whole mechanism: strictness arrives one directory at a time, each arrival",
    "is a visible line in a diff, and nothing can quietly regress because the",
    "folder it belongs to is already listed.",
    "",
    "`include` NOW COVERS src/**, and every .ts and .tsx in the tree is graded",
    "with noImplicitAny on. What keeps this file alive is checkJs: the 272",
    "remaining .js files are all browser files, they convert with the UI work in",
    "Wave 4, and this config and the allowJs escape hatch go with them.",
    "",
    "THE COUNT IS MEASURED, NOT REMEMBERED. It said 212 here and in the comment",
    "below while the tree held 272 — the browser files grew with four waves of",
    "screens and nothing re-counted, because nothing fails when a comment is",
    "wrong. Re-measure it when you touch this file:",
    "  find src/components src/app -name '*.js' -o -name '*.jsx' | wc -l"
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noImplicitAny": true,
    "checkJs": false,
    "incremental": false
  },
  "include": [
    // OVERRIDING `include` DROPS THE BASE CONFIG'S, which is where
    // next-env.d.ts lives — and that is what augments fetch's RequestInit with
    // Next's `next: { revalidate }`. Without it a route using the option fails
    // here and nowhere else, which reads as a bug in the route.
    "next-env.d.ts",
    // THE RATCHET HAS MET ITS END for TypeScript: every .ts and .tsx in the
    // tree is graded with noImplicitAny on. It arrived one folder at a time —
    // platform, then people and hr, then the ten departments, then src/lib —
    // and the folder list is gone because there is nothing left to add.
    //
    // What is left is `checkJs`, and that is the 272 remaining .js files, every
    // one of them a browser file. They convert with the UI work in Wave 4, and
    // this config and the allowJs escape hatch both go with them.
    "src/**/*"
  ]
}
