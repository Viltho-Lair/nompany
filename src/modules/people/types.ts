// PEOPLE'S TYPES — the records inferred from `schema.ts`.
//
// This module has no context of its own: People is a screen over collaborators
// and roles, and it runs on the plain studio context rather than on a
// department section. So there is nothing here but the re-export, and that is
// the honest content rather than a file waiting to be filled.

export type { JoinRequest, Role } from "./schema";
